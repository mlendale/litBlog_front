 <div class="center jumbotron">
    <h1>Welcome to the litBlog app</h1>
    
    {{#if session.isAuthenticated}}
    
    <p>What are you up to today?</p>
    
    <a href="{{currentUser.user.name}}/posts" >See your posts</a>
    <!-- {{link-to "Create new posts" 'posts/new' class="btn btn-lg btn-primary"}}
    {{link-to "Edit your profile" 'users/edit' class="btn btn-lg btn-primary"}} -->
    {{else}}
    
    
    <h2>This is a light blogging application.</h2>
    	<div class="alert alert-info">
			{{link-to "Sign up now!" 'users.new' class="btn btn-lg btn-primary"}}		
		</div>
	{{/if}}
    
</div>
